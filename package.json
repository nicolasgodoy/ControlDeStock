{
    "name": "app-control-stock",
    "version": "1.0.0",
    "description": "Stock Control Application with Obfuscated JS",
    "main": "js/app.js",
    "scripts": {
        "clean": "if exist dist rmdir /s /q dist",
        "build": "npm run clean && mkdir dist && mkdir dist\\js && javascript-obfuscator src --output dist/js --compact true && copy index.html dist\\index.html && copy estilos.css dist\\estilos.css"
    },
    "devDependencies": {
        "javascript-obfuscator": "^4.2.2"
    }
}